client
Quien hace la request o solicitud

request
La consulta o lo que enviamos para consultar el API

server
Quien atiende o responde la solicitud

response
Lo que devuel el API

contrato
Reglas para comunicarse con el API
    * URL
    * Method
    * Params
    * Body
    * Myme Types

Protocolo HTTP
Reglas comunicacion en internet

Backend
Establece el contrato del API

Frontend
Entender y seguir el contrato de las apis

VERBOS HTTP
GET, POST, PUT, PATCH y DELETE

Metodos para trabajar json y string
JSON.stringify
JSON.parse

Manejo de errores
try, catch, if, else 


Routing / Routes
Direccionar a distintas partes del codigo segun la URL


URL que invocamos de una API se le conoce como endpoints


FLUJO GIT EN EQUIPO

1. Creacion del repo y asignacion permisos
2. Commit inicial que todas bajan
3. Dev 1 sube cambios (add, commit y push)
4. Dev 2 (add, commit, pull, push)
5. En el punto 4 si hay conflictos de deben arreglar.

NOTA
git flow 
trabajo en equipo en git
resolucion de conflictos en git